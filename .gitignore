# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files & secrets (never commit tokens)
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# scrape working dir + screenshots (source assets already copied into /public)
/_scrape
/_shots

# ignore all env files EXCEPT the public example (keep this rule last so it wins)
.env*
!.env.example
